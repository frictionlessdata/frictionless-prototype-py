name: pipeline
type: dataflows
steps:
  - type: load
    body:
      loadSource: 'data/table.csv'
  - type: set_type
    body:
      name: id
      type: string
  - type: dump_to_path
    body:
      outPath: 'tmp/pipeline'
      prettyDescriptor: true
